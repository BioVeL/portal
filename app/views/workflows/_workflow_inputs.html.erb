<div id="wf_inputs" style="height:300px;overflow:auto;">
<% if @sources.count>0 %>
   <p>
     <b> This workflow has <%= @sources.count %> <%= "Input".pluralize(@sources.count)%> </b>
   </p>
   <% @sources.each do |item| %> 
    <p>
        <% portname = item[0] %>
        <b><%= label_tag portname %></b> 
        <%= simple_format( @source_descriptions[portname] )%>
        <% unless @sources[portname].nil?|| @sinks[portname]=="" %>
          <b>Example Value:</b> <%= simple_format(@sources[portname]) %>
        <%end%>
    </p>
    <% end %>

<% else %>  
  <p>
    <b> This workflow has no inputs </b>
  </p>
<% end %>
</div>  

